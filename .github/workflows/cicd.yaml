name: CICD Pipeline
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout library
        uses: actions/checkout@v3
      - name: List all the files
        run : ls -la